# PHPMailer (vendored)

PHPMailer v6.9.3 — included directly (no Composer required).

Mail integration will be wired up in a later phase. Core classes live under `src/`:

```
includes/PHPMailer/
└── src/
    ├── PHPMailer.php
    ├── SMTP.php
    └── Exception.php
```
